import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tej.tech',
  appName: 'tejtech',
  webDir: 'dist/myfinance',
  bundledWebRuntime: false
};

export default config;
